import PATHS_CONFIG from "@/utils/configurations/paths-config";
import COOKIES_CONFIG from "@/utils/configurations/cookies-config";
import THEMES_CONFIG from "@/utils/configurations/themes-config";
import MESSAGE_CONFIG from "@/utils/configurations/message-config";
import SERVER_STATUS_CODES from "@/utils/configurations/server-status-codes";

const CONFIG = {
    PATHS_CONFIG,
    COOKIES_CONFIG,
    THEMES_CONFIG,
    MESSAGE_CONFIG,
    SERVER_STATUS_CODES,
};

export default CONFIG;